<?php

/**
 * Base class that represents a row from the 'featurevector' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Wed May 30 15:27:45 2012
 *
 * @package    lib.model.smafe.om
 */
abstract class BaseFeaturevector extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        FeaturevectorPeer
	 */
	protected static $peer;

	/**
	 * The value for the track_id field.
	 * @var        int
	 */
	protected $track_id;

	/**
	 * The value for the featurevectortype_id field.
	 * @var        int
	 */
	protected $featurevectortype_id;

	/**
	 * The value for the data field.
	 * @var        resource
	 */
	protected $data;

	/**
	 * The value for the file_id field.
	 * @var        int
	 */
	protected $file_id;

	/**
	 * The value for the inserted field.
	 * @var        string
	 */
	protected $inserted;

	/**
	 * The value for the updated field.
	 * @var        string
	 */
	protected $updated;

	/**
	 * @var        Track
	 */
	protected $aTrack;

	/**
	 * @var        Featurevectortype
	 */
	protected $aFeaturevectortype;

	/**
	 * @var        File
	 */
	protected $aFile;

	/**
	 * @var        array Distance[] Collection to store aggregation of Distance objects.
	 */
	protected $collDistancesRelatedByTrackAId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDistancesRelatedByTrackAId.
	 */
	private $lastDistanceRelatedByTrackAIdCriteria = null;

	/**
	 * @var        array Distance[] Collection to store aggregation of Distance objects.
	 */
	protected $collDistancesRelatedByTrackBId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDistancesRelatedByTrackBId.
	 */
	private $lastDistanceRelatedByTrackBIdCriteria = null;

	/**
	 * @var        array Distancejob[] Collection to store aggregation of Distancejob objects.
	 */
	protected $collDistancejobs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDistancejobs.
	 */
	private $lastDistancejobCriteria = null;

	/**
	 * @var        array Featurevectorsegment[] Collection to store aggregation of Featurevectorsegment objects.
	 */
	protected $collFeaturevectorsegments;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFeaturevectorsegments.
	 */
	private $lastFeaturevectorsegmentCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'FeaturevectorPeer';

	/**
	 * Get the [track_id] column value.
	 * 
	 * @return     int
	 */
	public function getTrackId()
	{
		return $this->track_id;
	}

	/**
	 * Get the [featurevectortype_id] column value.
	 * 
	 * @return     int
	 */
	public function getFeaturevectortypeId()
	{
		return $this->featurevectortype_id;
	}

	/**
	 * Get the [data] column value.
	 * 
	 * @return     resource
	 */
	public function getData()
	{
		return $this->data;
	}

	/**
	 * Get the [file_id] column value.
	 * 
	 * @return     int
	 */
	public function getFileId()
	{
		return $this->file_id;
	}

	/**
	 * Get the [optionally formatted] temporal [inserted] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getInserted($format = 'Y-m-d H:i:s')
	{
		if ($this->inserted === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->inserted);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->inserted, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdated($format = 'Y-m-d H:i:s')
	{
		if ($this->updated === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->updated);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [track_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Featurevector The current object (for fluent API support)
	 */
	public function setTrackId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->track_id !== $v) {
			$this->track_id = $v;
			$this->modifiedColumns[] = FeaturevectorPeer::TRACK_ID;
		}

		if ($this->aTrack !== null && $this->aTrack->getId() !== $v) {
			$this->aTrack = null;
		}

		return $this;
	} // setTrackId()

	/**
	 * Set the value of [featurevectortype_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Featurevector The current object (for fluent API support)
	 */
	public function setFeaturevectortypeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->featurevectortype_id !== $v) {
			$this->featurevectortype_id = $v;
			$this->modifiedColumns[] = FeaturevectorPeer::FEATUREVECTORTYPE_ID;
		}

		if ($this->aFeaturevectortype !== null && $this->aFeaturevectortype->getId() !== $v) {
			$this->aFeaturevectortype = null;
		}

		return $this;
	} // setFeaturevectortypeId()

	/**
	 * Set the value of [data] column.
	 * 
	 * @param      resource $v new value
	 * @return     Featurevector The current object (for fluent API support)
	 */
	public function setData($v)
	{
		// Because BLOB columns are streams in PDO we have to assume that they are
		// always modified when a new value is passed in.  For example, the contents
		// of the stream itself may have changed externally.
		if (!is_resource($v) && $v !== null) {
			$this->data = fopen('php://memory', 'r+');
			fwrite($this->data, $v);
			rewind($this->data);
		} else { // it's already a stream
			$this->data = $v;
		}
		$this->modifiedColumns[] = FeaturevectorPeer::DATA;

		return $this;
	} // setData()

	/**
	 * Set the value of [file_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Featurevector The current object (for fluent API support)
	 */
	public function setFileId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->file_id !== $v) {
			$this->file_id = $v;
			$this->modifiedColumns[] = FeaturevectorPeer::FILE_ID;
		}

		if ($this->aFile !== null && $this->aFile->getId() !== $v) {
			$this->aFile = null;
		}

		return $this;
	} // setFileId()

	/**
	 * Sets the value of [inserted] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Featurevector The current object (for fluent API support)
	 */
	public function setInserted($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->inserted !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->inserted !== null && $tmpDt = new DateTime($this->inserted)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->inserted = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = FeaturevectorPeer::INSERTED;
			}
		} // if either are not null

		return $this;
	} // setInserted()

	/**
	 * Sets the value of [updated] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Featurevector The current object (for fluent API support)
	 */
	public function setUpdated($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated !== null && $tmpDt = new DateTime($this->updated)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = FeaturevectorPeer::UPDATED;
			}
		} // if either are not null

		return $this;
	} // setUpdated()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->track_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->featurevectortype_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->data = $row[$startcol + 2];
			$this->file_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->inserted = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->updated = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 6; // 6 = FeaturevectorPeer::NUM_COLUMNS - FeaturevectorPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Featurevector object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aTrack !== null && $this->track_id !== $this->aTrack->getId()) {
			$this->aTrack = null;
		}
		if ($this->aFeaturevectortype !== null && $this->featurevectortype_id !== $this->aFeaturevectortype->getId()) {
			$this->aFeaturevectortype = null;
		}
		if ($this->aFile !== null && $this->file_id !== $this->aFile->getId()) {
			$this->aFile = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FeaturevectorPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = FeaturevectorPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aTrack = null;
			$this->aFeaturevectortype = null;
			$this->aFile = null;
			$this->collDistancesRelatedByTrackAId = null;
			$this->lastDistanceRelatedByTrackAIdCriteria = null;

			$this->collDistancesRelatedByTrackBId = null;
			$this->lastDistanceRelatedByTrackBIdCriteria = null;

			$this->collDistancejobs = null;
			$this->lastDistancejobCriteria = null;

			$this->collFeaturevectorsegments = null;
			$this->lastFeaturevectorsegmentCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FeaturevectorPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseFeaturevector:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				FeaturevectorPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseFeaturevector:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FeaturevectorPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseFeaturevector:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseFeaturevector:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				FeaturevectorPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTrack !== null) {
				if ($this->aTrack->isModified() || $this->aTrack->isNew()) {
					$affectedRows += $this->aTrack->save($con);
				}
				$this->setTrack($this->aTrack);
			}

			if ($this->aFeaturevectortype !== null) {
				if ($this->aFeaturevectortype->isModified() || $this->aFeaturevectortype->isNew()) {
					$affectedRows += $this->aFeaturevectortype->save($con);
				}
				$this->setFeaturevectortype($this->aFeaturevectortype);
			}

			if ($this->aFile !== null) {
				if ($this->aFile->isModified() || $this->aFile->isNew()) {
					$affectedRows += $this->aFile->save($con);
				}
				$this->setFile($this->aFile);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = FeaturevectorPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += FeaturevectorPeer::doUpdate($this, $con);
				}

				// Rewind the data LOB column, since PDO does not rewind after inserting value.
				if ($this->data !== null && is_resource($this->data)) {
					rewind($this->data);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collDistancesRelatedByTrackAId !== null) {
				foreach ($this->collDistancesRelatedByTrackAId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDistancesRelatedByTrackBId !== null) {
				foreach ($this->collDistancesRelatedByTrackBId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDistancejobs !== null) {
				foreach ($this->collDistancejobs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFeaturevectorsegments !== null) {
				foreach ($this->collFeaturevectorsegments as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTrack !== null) {
				if (!$this->aTrack->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTrack->getValidationFailures());
				}
			}

			if ($this->aFeaturevectortype !== null) {
				if (!$this->aFeaturevectortype->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aFeaturevectortype->getValidationFailures());
				}
			}

			if ($this->aFile !== null) {
				if (!$this->aFile->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aFile->getValidationFailures());
				}
			}


			if (($retval = FeaturevectorPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collDistancesRelatedByTrackAId !== null) {
					foreach ($this->collDistancesRelatedByTrackAId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDistancesRelatedByTrackBId !== null) {
					foreach ($this->collDistancesRelatedByTrackBId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDistancejobs !== null) {
					foreach ($this->collDistancejobs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFeaturevectorsegments !== null) {
					foreach ($this->collFeaturevectorsegments as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = FeaturevectorPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getTrackId();
				break;
			case 1:
				return $this->getFeaturevectortypeId();
				break;
			case 2:
				return $this->getData();
				break;
			case 3:
				return $this->getFileId();
				break;
			case 4:
				return $this->getInserted();
				break;
			case 5:
				return $this->getUpdated();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = FeaturevectorPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getTrackId(),
			$keys[1] => $this->getFeaturevectortypeId(),
			$keys[2] => $this->getData(),
			$keys[3] => $this->getFileId(),
			$keys[4] => $this->getInserted(),
			$keys[5] => $this->getUpdated(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = FeaturevectorPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setTrackId($value);
				break;
			case 1:
				$this->setFeaturevectortypeId($value);
				break;
			case 2:
				$this->setData($value);
				break;
			case 3:
				$this->setFileId($value);
				break;
			case 4:
				$this->setInserted($value);
				break;
			case 5:
				$this->setUpdated($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = FeaturevectorPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setTrackId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setFeaturevectortypeId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setData($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setFileId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setInserted($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setUpdated($arr[$keys[5]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);

		if ($this->isColumnModified(FeaturevectorPeer::TRACK_ID)) $criteria->add(FeaturevectorPeer::TRACK_ID, $this->track_id);
		if ($this->isColumnModified(FeaturevectorPeer::FEATUREVECTORTYPE_ID)) $criteria->add(FeaturevectorPeer::FEATUREVECTORTYPE_ID, $this->featurevectortype_id);
		if ($this->isColumnModified(FeaturevectorPeer::DATA)) $criteria->add(FeaturevectorPeer::DATA, $this->data);
		if ($this->isColumnModified(FeaturevectorPeer::FILE_ID)) $criteria->add(FeaturevectorPeer::FILE_ID, $this->file_id);
		if ($this->isColumnModified(FeaturevectorPeer::INSERTED)) $criteria->add(FeaturevectorPeer::INSERTED, $this->inserted);
		if ($this->isColumnModified(FeaturevectorPeer::UPDATED)) $criteria->add(FeaturevectorPeer::UPDATED, $this->updated);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);

		$criteria->add(FeaturevectorPeer::TRACK_ID, $this->track_id);
		$criteria->add(FeaturevectorPeer::FEATUREVECTORTYPE_ID, $this->featurevectortype_id);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getTrackId();

		$pks[1] = $this->getFeaturevectortypeId();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setTrackId($keys[0]);

		$this->setFeaturevectortypeId($keys[1]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Featurevector (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setTrackId($this->track_id);

		$copyObj->setFeaturevectortypeId($this->featurevectortype_id);

		$copyObj->setData($this->data);

		$copyObj->setFileId($this->file_id);

		$copyObj->setInserted($this->inserted);

		$copyObj->setUpdated($this->updated);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getDistancesRelatedByTrackAId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDistanceRelatedByTrackAId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDistancesRelatedByTrackBId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDistanceRelatedByTrackBId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDistancejobs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDistancejob($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFeaturevectorsegments() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFeaturevectorsegment($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Featurevector Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     FeaturevectorPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new FeaturevectorPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Track object.
	 *
	 * @param      Track $v
	 * @return     Featurevector The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTrack(Track $v = null)
	{
		if ($v === null) {
			$this->setTrackId(NULL);
		} else {
			$this->setTrackId($v->getId());
		}

		$this->aTrack = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Track object, it will not be re-added.
		if ($v !== null) {
			$v->addFeaturevector($this);
		}

		return $this;
	}


	/**
	 * Get the associated Track object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Track The associated Track object.
	 * @throws     PropelException
	 */
	public function getTrack(PropelPDO $con = null)
	{
		if ($this->aTrack === null && ($this->track_id !== null)) {
			$this->aTrack = TrackPeer::retrieveByPk($this->track_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTrack->addFeaturevectors($this);
			 */
		}
		return $this->aTrack;
	}

	/**
	 * Declares an association between this object and a Featurevectortype object.
	 *
	 * @param      Featurevectortype $v
	 * @return     Featurevector The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setFeaturevectortype(Featurevectortype $v = null)
	{
		if ($v === null) {
			$this->setFeaturevectortypeId(NULL);
		} else {
			$this->setFeaturevectortypeId($v->getId());
		}

		$this->aFeaturevectortype = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Featurevectortype object, it will not be re-added.
		if ($v !== null) {
			$v->addFeaturevector($this);
		}

		return $this;
	}


	/**
	 * Get the associated Featurevectortype object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Featurevectortype The associated Featurevectortype object.
	 * @throws     PropelException
	 */
	public function getFeaturevectortype(PropelPDO $con = null)
	{
		if ($this->aFeaturevectortype === null && ($this->featurevectortype_id !== null)) {
			$this->aFeaturevectortype = FeaturevectortypePeer::retrieveByPk($this->featurevectortype_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aFeaturevectortype->addFeaturevectors($this);
			 */
		}
		return $this->aFeaturevectortype;
	}

	/**
	 * Declares an association between this object and a File object.
	 *
	 * @param      File $v
	 * @return     Featurevector The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setFile(File $v = null)
	{
		if ($v === null) {
			$this->setFileId(NULL);
		} else {
			$this->setFileId($v->getId());
		}

		$this->aFile = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the File object, it will not be re-added.
		if ($v !== null) {
			$v->addFeaturevector($this);
		}

		return $this;
	}


	/**
	 * Get the associated File object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     File The associated File object.
	 * @throws     PropelException
	 */
	public function getFile(PropelPDO $con = null)
	{
		if ($this->aFile === null && ($this->file_id !== null)) {
			$this->aFile = FilePeer::retrieveByPk($this->file_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aFile->addFeaturevectors($this);
			 */
		}
		return $this->aFile;
	}

	/**
	 * Clears out the collDistancesRelatedByTrackAId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDistancesRelatedByTrackAId()
	 */
	public function clearDistancesRelatedByTrackAId()
	{
		$this->collDistancesRelatedByTrackAId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDistancesRelatedByTrackAId collection (array).
	 *
	 * By default this just sets the collDistancesRelatedByTrackAId collection to an empty array (like clearcollDistancesRelatedByTrackAId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDistancesRelatedByTrackAId()
	{
		$this->collDistancesRelatedByTrackAId = array();
	}

	/**
	 * Gets an array of Distance objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Featurevector has previously been saved, it will retrieve
	 * related DistancesRelatedByTrackAId from storage. If this Featurevector is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Distance[]
	 * @throws     PropelException
	 */
	public function getDistancesRelatedByTrackAId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancesRelatedByTrackAId === null) {
			if ($this->isNew()) {
			   $this->collDistancesRelatedByTrackAId = array();
			} else {

				$criteria->add(DistancePeer::TRACK_A_ID, $this->track_id);

				DistancePeer::addSelectColumns($criteria);
				$this->collDistancesRelatedByTrackAId = DistancePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DistancePeer::TRACK_A_ID, $this->track_id);

				DistancePeer::addSelectColumns($criteria);
				if (!isset($this->lastDistanceRelatedByTrackAIdCriteria) || !$this->lastDistanceRelatedByTrackAIdCriteria->equals($criteria)) {
					$this->collDistancesRelatedByTrackAId = DistancePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDistanceRelatedByTrackAIdCriteria = $criteria;
		return $this->collDistancesRelatedByTrackAId;
	}

	/**
	 * Returns the number of related Distance objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Distance objects.
	 * @throws     PropelException
	 */
	public function countDistancesRelatedByTrackAId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDistancesRelatedByTrackAId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DistancePeer::TRACK_A_ID, $this->track_id);

				$count = DistancePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DistancePeer::TRACK_A_ID, $this->track_id);

				if (!isset($this->lastDistanceRelatedByTrackAIdCriteria) || !$this->lastDistanceRelatedByTrackAIdCriteria->equals($criteria)) {
					$count = DistancePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDistancesRelatedByTrackAId);
				}
			} else {
				$count = count($this->collDistancesRelatedByTrackAId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Distance object to this object
	 * through the Distance foreign key attribute.
	 *
	 * @param      Distance $l Distance
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDistanceRelatedByTrackAId(Distance $l)
	{
		if ($this->collDistancesRelatedByTrackAId === null) {
			$this->initDistancesRelatedByTrackAId();
		}
		if (!in_array($l, $this->collDistancesRelatedByTrackAId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDistancesRelatedByTrackAId, $l);
			$l->setFeaturevectorRelatedByTrackAId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Featurevector is new, it will return
	 * an empty collection; or if this Featurevector has previously
	 * been saved, it will retrieve related DistancesRelatedByTrackAId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Featurevector.
	 */
	public function getDistancesRelatedByTrackAIdJoinDistancetype($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancesRelatedByTrackAId === null) {
			if ($this->isNew()) {
				$this->collDistancesRelatedByTrackAId = array();
			} else {

				$criteria->add(DistancePeer::TRACK_A_ID, $this->track_id);

				$this->collDistancesRelatedByTrackAId = DistancePeer::doSelectJoinDistancetype($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DistancePeer::TRACK_A_ID, $this->track_id);

			if (!isset($this->lastDistanceRelatedByTrackAIdCriteria) || !$this->lastDistanceRelatedByTrackAIdCriteria->equals($criteria)) {
				$this->collDistancesRelatedByTrackAId = DistancePeer::doSelectJoinDistancetype($criteria, $con, $join_behavior);
			}
		}
		$this->lastDistanceRelatedByTrackAIdCriteria = $criteria;

		return $this->collDistancesRelatedByTrackAId;
	}

	/**
	 * Clears out the collDistancesRelatedByTrackBId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDistancesRelatedByTrackBId()
	 */
	public function clearDistancesRelatedByTrackBId()
	{
		$this->collDistancesRelatedByTrackBId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDistancesRelatedByTrackBId collection (array).
	 *
	 * By default this just sets the collDistancesRelatedByTrackBId collection to an empty array (like clearcollDistancesRelatedByTrackBId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDistancesRelatedByTrackBId()
	{
		$this->collDistancesRelatedByTrackBId = array();
	}

	/**
	 * Gets an array of Distance objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Featurevector has previously been saved, it will retrieve
	 * related DistancesRelatedByTrackBId from storage. If this Featurevector is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Distance[]
	 * @throws     PropelException
	 */
	public function getDistancesRelatedByTrackBId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancesRelatedByTrackBId === null) {
			if ($this->isNew()) {
			   $this->collDistancesRelatedByTrackBId = array();
			} else {

				$criteria->add(DistancePeer::TRACK_B_ID, $this->track_id);

				DistancePeer::addSelectColumns($criteria);
				$this->collDistancesRelatedByTrackBId = DistancePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DistancePeer::TRACK_B_ID, $this->track_id);

				DistancePeer::addSelectColumns($criteria);
				if (!isset($this->lastDistanceRelatedByTrackBIdCriteria) || !$this->lastDistanceRelatedByTrackBIdCriteria->equals($criteria)) {
					$this->collDistancesRelatedByTrackBId = DistancePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDistanceRelatedByTrackBIdCriteria = $criteria;
		return $this->collDistancesRelatedByTrackBId;
	}

	/**
	 * Returns the number of related Distance objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Distance objects.
	 * @throws     PropelException
	 */
	public function countDistancesRelatedByTrackBId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDistancesRelatedByTrackBId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DistancePeer::TRACK_B_ID, $this->track_id);

				$count = DistancePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DistancePeer::TRACK_B_ID, $this->track_id);

				if (!isset($this->lastDistanceRelatedByTrackBIdCriteria) || !$this->lastDistanceRelatedByTrackBIdCriteria->equals($criteria)) {
					$count = DistancePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDistancesRelatedByTrackBId);
				}
			} else {
				$count = count($this->collDistancesRelatedByTrackBId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Distance object to this object
	 * through the Distance foreign key attribute.
	 *
	 * @param      Distance $l Distance
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDistanceRelatedByTrackBId(Distance $l)
	{
		if ($this->collDistancesRelatedByTrackBId === null) {
			$this->initDistancesRelatedByTrackBId();
		}
		if (!in_array($l, $this->collDistancesRelatedByTrackBId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDistancesRelatedByTrackBId, $l);
			$l->setFeaturevectorRelatedByTrackBId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Featurevector is new, it will return
	 * an empty collection; or if this Featurevector has previously
	 * been saved, it will retrieve related DistancesRelatedByTrackBId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Featurevector.
	 */
	public function getDistancesRelatedByTrackBIdJoinDistancetype($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancesRelatedByTrackBId === null) {
			if ($this->isNew()) {
				$this->collDistancesRelatedByTrackBId = array();
			} else {

				$criteria->add(DistancePeer::TRACK_B_ID, $this->track_id);

				$this->collDistancesRelatedByTrackBId = DistancePeer::doSelectJoinDistancetype($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DistancePeer::TRACK_B_ID, $this->track_id);

			if (!isset($this->lastDistanceRelatedByTrackBIdCriteria) || !$this->lastDistanceRelatedByTrackBIdCriteria->equals($criteria)) {
				$this->collDistancesRelatedByTrackBId = DistancePeer::doSelectJoinDistancetype($criteria, $con, $join_behavior);
			}
		}
		$this->lastDistanceRelatedByTrackBIdCriteria = $criteria;

		return $this->collDistancesRelatedByTrackBId;
	}

	/**
	 * Clears out the collDistancejobs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDistancejobs()
	 */
	public function clearDistancejobs()
	{
		$this->collDistancejobs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDistancejobs collection (array).
	 *
	 * By default this just sets the collDistancejobs collection to an empty array (like clearcollDistancejobs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDistancejobs()
	{
		$this->collDistancejobs = array();
	}

	/**
	 * Gets an array of Distancejob objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Featurevector has previously been saved, it will retrieve
	 * related Distancejobs from storage. If this Featurevector is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Distancejob[]
	 * @throws     PropelException
	 */
	public function getDistancejobs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancejobs === null) {
			if ($this->isNew()) {
			   $this->collDistancejobs = array();
			} else {

				$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

				DistancejobPeer::addSelectColumns($criteria);
				$this->collDistancejobs = DistancejobPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

				DistancejobPeer::addSelectColumns($criteria);
				if (!isset($this->lastDistancejobCriteria) || !$this->lastDistancejobCriteria->equals($criteria)) {
					$this->collDistancejobs = DistancejobPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDistancejobCriteria = $criteria;
		return $this->collDistancejobs;
	}

	/**
	 * Returns the number of related Distancejob objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Distancejob objects.
	 * @throws     PropelException
	 */
	public function countDistancejobs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDistancejobs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

				$count = DistancejobPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

				if (!isset($this->lastDistancejobCriteria) || !$this->lastDistancejobCriteria->equals($criteria)) {
					$count = DistancejobPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDistancejobs);
				}
			} else {
				$count = count($this->collDistancejobs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Distancejob object to this object
	 * through the Distancejob foreign key attribute.
	 *
	 * @param      Distancejob $l Distancejob
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDistancejob(Distancejob $l)
	{
		if ($this->collDistancejobs === null) {
			$this->initDistancejobs();
		}
		if (!in_array($l, $this->collDistancejobs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDistancejobs, $l);
			$l->setFeaturevector($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Featurevector is new, it will return
	 * an empty collection; or if this Featurevector has previously
	 * been saved, it will retrieve related Distancejobs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Featurevector.
	 */
	public function getDistancejobsJoinDistancetype($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancejobs === null) {
			if ($this->isNew()) {
				$this->collDistancejobs = array();
			} else {

				$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

				$this->collDistancejobs = DistancejobPeer::doSelectJoinDistancetype($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

			if (!isset($this->lastDistancejobCriteria) || !$this->lastDistancejobCriteria->equals($criteria)) {
				$this->collDistancejobs = DistancejobPeer::doSelectJoinDistancetype($criteria, $con, $join_behavior);
			}
		}
		$this->lastDistancejobCriteria = $criteria;

		return $this->collDistancejobs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Featurevector is new, it will return
	 * an empty collection; or if this Featurevector has previously
	 * been saved, it will retrieve related Distancejobs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Featurevector.
	 */
	public function getDistancejobsJoinSmafejobAddfile($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDistancejobs === null) {
			if ($this->isNew()) {
				$this->collDistancejobs = array();
			} else {

				$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

				$this->collDistancejobs = DistancejobPeer::doSelectJoinSmafejobAddfile($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DistancejobPeer::TRACK_ID, $this->track_id);

			if (!isset($this->lastDistancejobCriteria) || !$this->lastDistancejobCriteria->equals($criteria)) {
				$this->collDistancejobs = DistancejobPeer::doSelectJoinSmafejobAddfile($criteria, $con, $join_behavior);
			}
		}
		$this->lastDistancejobCriteria = $criteria;

		return $this->collDistancejobs;
	}

	/**
	 * Clears out the collFeaturevectorsegments collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFeaturevectorsegments()
	 */
	public function clearFeaturevectorsegments()
	{
		$this->collFeaturevectorsegments = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFeaturevectorsegments collection (array).
	 *
	 * By default this just sets the collFeaturevectorsegments collection to an empty array (like clearcollFeaturevectorsegments());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFeaturevectorsegments()
	{
		$this->collFeaturevectorsegments = array();
	}

	/**
	 * Gets an array of Featurevectorsegment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Featurevector has previously been saved, it will retrieve
	 * related Featurevectorsegments from storage. If this Featurevector is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Featurevectorsegment[]
	 * @throws     PropelException
	 */
	public function getFeaturevectorsegments($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFeaturevectorsegments === null) {
			if ($this->isNew()) {
			   $this->collFeaturevectorsegments = array();
			} else {

				$criteria->add(FeaturevectorsegmentPeer::TRACK_ID, $this->track_id);

				FeaturevectorsegmentPeer::addSelectColumns($criteria);
				$this->collFeaturevectorsegments = FeaturevectorsegmentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FeaturevectorsegmentPeer::TRACK_ID, $this->track_id);

				FeaturevectorsegmentPeer::addSelectColumns($criteria);
				if (!isset($this->lastFeaturevectorsegmentCriteria) || !$this->lastFeaturevectorsegmentCriteria->equals($criteria)) {
					$this->collFeaturevectorsegments = FeaturevectorsegmentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFeaturevectorsegmentCriteria = $criteria;
		return $this->collFeaturevectorsegments;
	}

	/**
	 * Returns the number of related Featurevectorsegment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Featurevectorsegment objects.
	 * @throws     PropelException
	 */
	public function countFeaturevectorsegments(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFeaturevectorsegments === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FeaturevectorsegmentPeer::TRACK_ID, $this->track_id);

				$count = FeaturevectorsegmentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FeaturevectorsegmentPeer::TRACK_ID, $this->track_id);

				if (!isset($this->lastFeaturevectorsegmentCriteria) || !$this->lastFeaturevectorsegmentCriteria->equals($criteria)) {
					$count = FeaturevectorsegmentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFeaturevectorsegments);
				}
			} else {
				$count = count($this->collFeaturevectorsegments);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Featurevectorsegment object to this object
	 * through the Featurevectorsegment foreign key attribute.
	 *
	 * @param      Featurevectorsegment $l Featurevectorsegment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFeaturevectorsegment(Featurevectorsegment $l)
	{
		if ($this->collFeaturevectorsegments === null) {
			$this->initFeaturevectorsegments();
		}
		if (!in_array($l, $this->collFeaturevectorsegments, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFeaturevectorsegments, $l);
			$l->setFeaturevector($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Featurevector is new, it will return
	 * an empty collection; or if this Featurevector has previously
	 * been saved, it will retrieve related Featurevectorsegments from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Featurevector.
	 */
	public function getFeaturevectorsegmentsJoinFile($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FeaturevectorPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFeaturevectorsegments === null) {
			if ($this->isNew()) {
				$this->collFeaturevectorsegments = array();
			} else {

				$criteria->add(FeaturevectorsegmentPeer::TRACK_ID, $this->track_id);

				$this->collFeaturevectorsegments = FeaturevectorsegmentPeer::doSelectJoinFile($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FeaturevectorsegmentPeer::TRACK_ID, $this->track_id);

			if (!isset($this->lastFeaturevectorsegmentCriteria) || !$this->lastFeaturevectorsegmentCriteria->equals($criteria)) {
				$this->collFeaturevectorsegments = FeaturevectorsegmentPeer::doSelectJoinFile($criteria, $con, $join_behavior);
			}
		}
		$this->lastFeaturevectorsegmentCriteria = $criteria;

		return $this->collFeaturevectorsegments;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collDistancesRelatedByTrackAId) {
				foreach ((array) $this->collDistancesRelatedByTrackAId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDistancesRelatedByTrackBId) {
				foreach ((array) $this->collDistancesRelatedByTrackBId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDistancejobs) {
				foreach ((array) $this->collDistancejobs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFeaturevectorsegments) {
				foreach ((array) $this->collFeaturevectorsegments as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collDistancesRelatedByTrackAId = null;
		$this->collDistancesRelatedByTrackBId = null;
		$this->collDistancejobs = null;
		$this->collFeaturevectorsegments = null;
			$this->aTrack = null;
			$this->aFeaturevectortype = null;
			$this->aFile = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseFeaturevector:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseFeaturevector::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseFeaturevector
